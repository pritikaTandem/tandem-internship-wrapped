export type Photo = { src: string; caption: string };

/** Running tag stamped on every polaroid, not per-photo. */
export const PHOTOS_HASHTAG = "#tandeminternshipwrapped";

/** Drop files in /public/photos and list them here. */
export const PHOTOS: Photo[] = [];
